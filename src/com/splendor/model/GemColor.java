package com.splendor.model;

// public enumeration used which is a special class that represents a fixed set of constants to hold our Gem colors.
public enum  GemColor {
    WHITE,
    BLUE,
    GREEN,
    RED,
    BLACK,
    GOLD // Joker/wildcard
}
